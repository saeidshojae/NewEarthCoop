# 📦 راهنمای استفاده از اسکریپت Archive

این راهنما توضیح می‌دهد چگونه از اسکریپت‌های انتقال فایل‌ها به پوشه `_archive` استفاده کنید.

## 🎯 هدف

به جای حذف مستقیم فایل‌ها، آنها را به پوشه `_archive` منتقل می‌کنیم تا:
- ✅ فایل‌ها از دست نروند
- ✅ بتوانید بعداً بررسی کنید
- ✅ اگر اشتباهی بود، بتوانید برگردانید
- ✅ پروژه تمیز شود

## 📋 قبل از اجرا

1. **Backup بگیرید:** همیشه قبل از هر تغییری یک backup از پروژه داشته باشید
2. **Git Commit کنید:** اگر تغییرات commit نشده‌اند، ابتدا commit کنید
3. **بررسی کنید:** فایل `CLEANUP_RECOMMENDATIONS.md` را بخوانید

## 🚀 نحوه اجرا

### Windows (PowerShell):

```powershell
# اجرای اسکریپت
.\archive_files.ps1
```

اگر خطای permission دریافت کردید:
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
.\archive_files.ps1
```

### Linux/Mac (Bash):

```bash
# دادن دسترسی اجرا
chmod +x archive_files.sh

# اجرای اسکریپت
./archive_files.sh
```

## 📁 ساختار پوشه _archive

بعد از اجرای اسکریپت، ساختار زیر ایجاد می‌شود:

```
_archive/
├── README.md                    # راهنمای پوشه archive
├── backup-old-files/           # فایل‌های backup و old
├── temp-files/                 # فایل‌های موقت
├── test-files/                 # فایل‌های تست
├── sql-backups/                # فایل‌های SQL backup
├── duplicate-files/            # فایل‌های duplicate
├── macosx-folder/              # پوشه __MACOSX
├── new-ui-folder/              # پوشه New ui و group-chat-redesign
├── check-scripts/              # اسکریپت‌های check و import
└── migration-skip/             # فایل‌های migration .skip
```

## ✅ بعد از اجرا

1. **بررسی کنید:** پوشه `_archive` را بررسی کنید و مطمئن شوید فایل‌های درست منتقل شده‌اند
2. **تست کنید:** پروژه را اجرا کنید و مطمئن شوید همه چیز کار می‌کند
3. **Git Commit:** تغییرات را commit کنید:
   ```bash
   git add _archive/
   git add archive_files.ps1 archive_files.sh ARCHIVE_GUIDE.md
   git commit -m "Archive unused files for review"
   ```

## 🔄 برگرداندن فایل‌ها

اگر متوجه شدید فایلی اشتباهی منتقل شده، می‌توانید آن را برگردانید:

### Windows (PowerShell):
```powershell
# مثال: برگرداندن یک فایل
Move-Item "_archive\backup-old-files\home.blade.php.backup" "resources\views\home.blade.php.backup"
```

### Linux/Mac (Bash):
```bash
# مثال: برگرداندن یک فایل
mv "_archive/backup-old-files/home.blade.php.backup" "resources/views/home.blade.php.backup"
```

## 🗑️ حذف نهایی

بعد از بررسی و اطمینان از اینکه فایل‌ها دیگر نیاز نیستند:

### حذف کل پوشه _archive:
```bash
# Windows
Remove-Item -Recurse -Force "_archive"

# Linux/Mac
rm -rf _archive
```

### حذف تدریجی:
می‌توانید به تدریج هر دسته را بررسی و حذف کنید.

## ⚠️ نکات مهم

1. **فایل‌های تست:** فایل `test-unified-layout.blade.php` در `routes/web.php` استفاده می‌شود. قبل از حذف، route را بررسی کنید.

2. **اسکریپت‌های Check:** اگر این اسکریپت‌ها برای maintenance استفاده می‌شوند، آنها را نگه دارید.

3. **فایل‌های SQL:** فایل `ybwztpvr_earth (7).sql` یک backup دیتابیس است و نباید در repository باشد.

4. **پوشه New ui:** این پوشه شامل فایل‌های HTML استاتیک است. اگر مطمئن نیستید، نگه دارید.

## 📝 یادداشت

- اسکریپت‌ها فایل‌ها را **منتقل** می‌کنند نه کپی
- اگر فایلی در مقصد وجود داشته باشد، timestamp اضافه می‌شود
- تمام فایل‌ها در پوشه‌های دسته‌بندی شده قرار می‌گیرند

## 🆘 مشکل دارید؟

اگر مشکلی پیش آمد:
1. فایل `CLEANUP_RECOMMENDATIONS.md` را بررسی کنید
2. فایل `README.md` در پوشه `_archive` را بخوانید
3. از Git برای برگرداندن تغییرات استفاده کنید

---

**آخرین بروزرسانی:** 2025-01-XX



