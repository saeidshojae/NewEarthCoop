# 📋 راهنمای کامل صفحه Home - New Earth Coop

## ✨ مقدمه و خلاصه تغییرات

صفحه اصلی (Home) سیستم کوپراتیو زمین نو با استفاده از **طرح کامل طراح UI** و ادغام تمامی لینک‌ها و منوهای سیستم قدیمی بازطراحی شد. این صفحه شامل هدر پیشرفته، سایدبار کامل، و محتوای اصلی با طراحی مدرن است.

---

## 🎯 ویژگی‌های اضافه شده در نسخه نهایی

### 1. **هدر کامل با تمام لینک‌های قدیمی**

#### منوی دسکتاپ:
- ✅ **خانه** - لینک به صفحه اصلی
- ✅ **وبلاگ** - لینک به `route('blog.index')`
- ✅ **دفتر سهام** - لینک به `route('auction.index')`
- ✅ **صفحات دینامیک** - نمایش تمام صفحات Published از دیتابیس

#### دکمه بازگشت:
- ✅ آیکون فلش بازگشت به صفحه قبل (یا Home)

#### لوگوی انیمیشن‌دار:
- ✅ SVG با انیمیشن Bounce

### 2. **دراپ‌داون کاربری کامل**

**بخش اول - پروفایل:**
- ✅ پروفایل (`profile.show`)

**بخش دوم - دفتر سهام:**
- ✅ حراج‌های سهام (`auction.index`)
- ✅ کیف‌پول (`wallet.index`)
- ✅ کیف‌سهام (`holding.index`)

**بخش سوم - اسناد:**
- ✅ اساسنامه (`terms`)
- ✅ توافقنامه مالی (`spring-accounts`)

**بخش چهارم - مدیریت (فقط ادمین):**
- ✅ پنل مدیریت (`admin.dashboard`)
- ✅ مدیریت وبلاگ (`admin.blog.dashboard`)

**بخش پنجم:**
- ✅ خروج (`logout`)

### 3. **سایدبار کامل با تمام آیتم‌ها**

**ترتیب منوها (10 آیتم):**

1. **اعلان‌ها** 🔔
   - لینک: `notifications.index`
   - Badge: تعداد اعلان‌های خوانده‌نشده (قرمز + Pulse)
   - آیکون: `fas fa-bell` (آبی)

2. **گروه‌های من** 👥
   - لینک: `groups.index`
   - Badge: تعداد گروه‌ها (طلایی)
   - آیکون: `fas fa-users` (آبی)

3. **مشارکت‌های من** 🤝
   - لینک: `history.index`
   - آیکون: `fas fa-handshake` (طلایی)

4. **انتخابات جاری** 🗳️
   - لینک: `history.election`
   - آیکون: `fas fa-vote-yea` (سبز)

5. **نظرسنجی‌های جاری** 📊
   - لینک: `history.poll`
   - آیکون: `fas fa-chart-pie` (آبی)

6. **حساب مالی نجم بهار** 💰
   - لینک: `spring-accounts`
   - Blinking: در صورت غیرفعال بودن
   - آیکون: `fas fa-wallet` (طلایی)

7. **دعوت از دوستان** ➕
   - لینک: `my-invation-code`
   - آیکون: `fas fa-user-plus` (سبز)

8. **ویرایش حساب کاربری** ⚙️
   - لینک: `profile.edit`
   - آیکون: `fas fa-cog` (آبی)

9. **پنل مدیریت** 🛡️ (فقط ادمین)
   - لینک: `admin.dashboard`
   - آیکون: `fas fa-user-shield` (بنفش)

10. **خروج** 🚪
    - لینک: `logout`
    - آیکون: `fas fa-sign-out-alt` (طلایی)

### 4. **منوی موبایل**

- ✅ همبرگر منو برای باز کردن
- ✅ نمایش تمام لینک‌های Header
- ✅ انیمیشن Slide-down
- ✅ بسته شدن با کلیک روی X

### 5. **محتوای اصلی**

#### بخش خوش‌آمدگویی:
- ✅ عنوان از Settings (`home_titre`)
- ✅ توضیح زیر عنوان

#### اسلایدر تصاویر:
- ✅ Swiper با Auto-play
- ✅ Pagination سبز
- ✅ Hover: Scale 1.05

#### محتوای HTML:
- ✅ از Settings (`home_content`)

#### آمار گروه‌ها:
- ✅ **گروه‌های عمومی** (سبز + آیکون users)
- ✅ **گروه‌های تخصصی** (آبی + آیکون briefcase)
- ✅ **گروه‌های اختصاصی** (بنفش + آیکون star)

#### حراج‌های فعال:
- ✅ نمایش 3 حراج
- ✅ زمان پایان (diffForHumans)
- ✅ دکمه مشاهده جزئیات

---

## 🎨 طراحی بصری و رنگ‌بندی

### رنگ‌ها (طبق طراح):
```css
--color-earth-green: #10b981     /* سبز زمینی */
--color-ocean-blue: #3b82f6      /* آبی اقیانوسی */
--color-digital-gold: #f59e0b    /* طلایی دیجیتال */
--color-pure-white: #ffffff      /* سفید خالص */
--color-light-gray: #f8fafc      /* خاکستری روشن */
--color-gentle-black: #1e293b    /* مشکی ملایم */
--color-dark-green: #047857      /* سبز تیره */
--color-dark-blue: #1d4ed8       /* آبی تیره */
--color-red-tomato: #FF6347      /* قرمز گوجه‌ای */
```

### فونت‌ها:
- **Vazirmatn**: برای فارسی (Google Fonts Iran)
- **Poppins**: برای انگلیسی

### افکت‌های ویژه:
1. **Glass Effect**: شیشه‌ای با Backdrop Blur
2. **Ripple Effect**: موج انتشار روی کلیک
3. **Float Animation**: حرکت شناور لوگو
4. **Pulse Animation**: تپش Badge اعلان‌ها
5. **Blink Animation**: چشمک‌زن برای Spring Account
6. **Hover Effects**: حرکت و سایه روی کارت‌ها

---

## 🔧 تکنولوژی‌های استفاده‌شده

### Frontend:
- **Tailwind CSS 3.x**: استایل اصلی
- **Alpine.js 3.x**: تعاملات سمت کلاینت
- **FontAwesome 6.0**: آیکون‌ها
- **Swiper 11.x**: اسلایدر تصاویر

### Backend:
- **Laravel Blade**: موتور تمپلیت
- **Eloquent ORM**: دریافت داده‌ها
- **Session Flash**: مدیریت پیام‌ها

---

## 📂 فایل‌های مرتبط و ساختار

### فایل‌های اصلی:
1. **resources/views/home.blade.php** - صفحه اصلی فعال
2. **resources/views/home-old-backup.blade.php** - نسخه پشتیبان قدیمی
3. **resources/views/home-complete.blade.php** - نسخه کامل (backup)
4. **resources/views/home-new.blade.php** - نسخه جدید (تست)
5. **app/Http/Controllers/HomeController.php** - کنترلر

### داده‌های ارسالی به View:
- `$groups`: گروه‌های کاربر
- `$activeAuctions`: حراج‌های فعال
- `$sliders`: اسلایدرهای Position 1
- تنظیمات Home (`home_titre`, `home_content`)

---

## 🔗 لینک‌های کامل سیستم

### هدر (Header):
1. خانه: `route('home')`
2. وبلاگ: `route('blog.index')`
3. دفتر سهام: `route('auction.index')`
4. صفحات دینامیک: `url('/pages/' . $page->slug)`

### دراپ‌داون کاربری:
1. پروفایل: `route('profile.show')`
2. حراج‌های سهام: `route('auction.index')`
3. کیف‌پول: `route('wallet.index')`
4. کیف‌سهام: `route('holding.index')`
5. اساسنامه: `route('terms')`
6. توافقنامه مالی: `route('spring-accounts')`
7. پنل مدیریت: `route('admin.dashboard')`
8. مدیریت وبلاگ: `route('admin.blog.dashboard')`
9. خروج: `route('logout')`

### سایدبار:
1. اعلان‌ها: `route('notifications.index')`
2. گروه‌های من: `route('groups.index')`
3. مشارکت‌ها: `route('history.index')`
4. انتخابات: `route('history.election')`
5. نظرسنجی: `route('history.poll')`
6. نجم بهار: `route('spring-accounts')`
7. دعوت: `route('my-invation-code')`
8. ویرایش: `route('profile.edit')`
9. پنل مدیریت: `route('admin.dashboard')`
10. خروج: `route('logout')`

---

## 🎭 انیمیشن‌ها و افکت‌ها

### Logo:
- **bounce-custom**: حرکت بالا-پایین (3 ثانیه)

### Badge:
- **pulse**: تپش نور (2 ثانیه)

### Spring Account:
- **blink**: چشمک‌زن (2 ثانیه) - در صورت غیرفعال بودن

### Sidebar Items:
- **fadeInUp**: ظاهر شدن از راست (0.1s - 0.9s تاخیر)

### Hover Effects:
- **Sidebar**: translateX + shadow
- **Cards**: translateY + shadow
- **Slider**: scale 1.05

---

## 📱 طراحی ریسپانسیو

### Desktop (≥1024px):
- سایدبار sticky در کنار محتوا
- هدر کامل با تمام منوها
- 3 ستون برای کارت‌های آمار

### Tablet (768px - 1023px):
- سایدبار fixed با دکمه باز/بسته
- 2 ستون برای کارت‌ها

### Mobile (<768px):
- سایدبار full-width
- منوی موبایل (همبرگر)
- تک ستون برای کارت‌ها
- دکمه Toggle سایدبار

---

## ✅ تست‌ها و تایید نهایی

### بررسی فایل فعال:
- ✅ HomeController از `view('home')` استفاده می‌کند
- ✅ ساختار صفحه با طراحی مطابقت دارد
- ✅ استایل‌ها و رنگ‌ها یکسان است
- ✅ فونت‌ها صحیح لود می‌شوند

### تست‌های انجام شده:
✅ تمام لینک‌های هدر  
✅ تمام آیتم‌های دراپ‌داون  
✅ تمام لینک‌های سایدبار  
✅ Badge اعلان‌ها  
✅ Blinking نجم بهار  
✅ منوی موبایل  
✅ Responsive تمام سایزها  
✅ انیمیشن‌ها  
✅ پیام‌های Success/Error  

---

## 🚀 نحوه استفاده

### دسترسی:
```
http://localhost:8000/home
```

### روت Laravel:
```php
Route::get('/home', [HomeController::class, 'index'])->name('home');
```

### پاکسازی Cache:
```powershell
php artisan view:clear
php artisan cache:clear
```

### بازگشت به نسخه قدیمی:
```powershell
Copy-Item "home-old-backup.blade.php" "home.blade.php" -Force
```

---

## 📝 نکات مهم برای توسعه‌دهندگان

### 1. چک کردن Admin:
```php
@if (auth()->user()->is_admin == 1)
    <!-- نمایش پنل مدیریت -->
@endif
```

### 2. Badge اعلان‌ها:
```php
@if(auth()->user()->unreadNotifications->count() > 0)
    <span>{{ auth()->user()->unreadNotifications->count() }}</span>
@endif
```

### 3. Blinking نجم بهار:
```php
@php
    $checkAcceptSpringAccount = \App\Models\Spring::where('user_id', auth()->user()->id)->first();
@endphp
<a class="{{ $checkAcceptSpringAccount && $checkAcceptSpringAccount->status == 0 ? 'blinking-item' : '' }}">
```

### 4. صفحات دینامیک:
```php
@foreach(\App\Models\Page::where('is_published', 1)->get() as $page)
    <a href="{{ url('/pages/' . $page->slug) }}">{{ $page->title }}</a>
@endforeach
```

### 5. افزودن آیتم به سایدبار:
```html
<a href="{{ route('your.route') }}" class="flex items-center gap-3 px-4 py-3 text-gray-700 hover:bg-gradient-to-r hover:from-blue-50 hover:to-blue-100 hover:text-blue-600 rounded-xl transition-all ripple">
    <i class="fas fa-icon w-5"></i>
    <span class="font-medium">عنوان آیتم</span>
</a>
```

---

## 📊 مقایسه با نسخه‌های قبلی

| ویژگی | نسخه اول | نسخه نهایی |
|-------|---------|------------|
| لینک‌های هدر | محدود | کامل (وبلاگ + دفتر سهام + صفحات) |
| دراپ‌داون کاربری | ساده | کامل (9 آیتم + تفکیک بخش‌ها) |
| سایدبار | 8 آیتم | 10 آیتم (با اعلان‌ها و پنل مدیریت) |
| منوی موبایل | ندارد | دارد |
| Badge اعلان‌ها | ندارد | دارد (Pulse) |
| دکمه بازگشت | ندارد | دارد |
| صفحات دینامیک | ندارد | دارد |
| Framework CSS | Bootstrap | Tailwind CSS |
| JavaScript | jQuery | Alpine.js |
| انیمیشن‌ها | محدود | کامل |

---

## 🐛 رفع مشکلات احتمالی

### مشکل: سایدبار نمایش داده نمی‌شود
**راه‌حل**: Alpine.js بررسی کنید (CDN لود شده باشد)

### مشکل: لوگو انیمیشن ندارد
**راه‌حل**: CSS Keyframes بررسی کنید

### مشکل: Badge اعلان‌ها نمایش داده نمی‌شود
**راه‌حل**: رابطه `unreadNotifications` در مدل User بررسی کنید

### مشکل: اسلایدر کار نمی‌کند
**راه‌حل**: Swiper CDN بررسی کنید و Position اسلایدرها در دیتابیس

### مشکل: فونت فارسی نمایش داده نمی‌شود
**راه‌حل**: لینک Google Fonts Iran بررسی کنید

---

## 📚 یادگیری از این پروژه

### مفاهیم کاربردی:
1. **Glass Morphism Design**
2. **Alpine.js State Management**
3. **Tailwind CSS Utility-First**
4. **SVG Animations با CSS**
5. **Responsive Sidebar Pattern**
6. **Badge System با Pulse Animation**
7. **Toast Notifications**
8. **Blade Component Integration**

---

## 📝 پیشنهادات آینده

- [ ] افزودن Dark Mode
- [ ] افزودن Search Bar در سایدبار
- [ ] افزودن Notifications Dropdown
- [ ] افزودن Widget‌های آماری بیشتر
- [ ] افزودن Calendar Widget
- [ ] افزودن Recent Activities
- [ ] بهینه‌سازی Performance (Lazy Loading)
- [ ] افزودن PWA Support

---

## 👥 مشارکت‌کنندگان

- **طراح UI**: ارائه طرح اولیه (index.html)
- **توسعه‌دهنده**: GitHub Copilot
- **تست و بررسی**: تیم NewEarthCoop

---

## 📞 پشتیبانی

در صورت بروز مشکل:
1. Cache Laravel پاک شود
2. Browser refresh شود (Ctrl+F5)
3. از نسخه backup استفاده شود
4. فایل‌های CDN بررسی شوند

---

**تاریخ ایجاد راهنما**: 2026-01-28  
**نسخه**: 3.0 (Final)  
**وضعیت**: ✅ آماده تولید و استفاده