# راهنمای کامل نجم‌هدا - نسخه 1.0.0
**تاریخ انتشار: ۲۸ ژانویه ۲۰۲۶**

---

# 🌟 راهنمای کامل نجم‌هدا

## معرفی نجم‌هدا

نجم‌هدا یک دستیار هوشمند چندعاملی است که برای کمک به کاربران و مدیران سیستم طراحی شده. این سیستم از پنج عامل تخصصی تشکیل شده و قابلیت خودتوسعه را دارد.

### عوامل موجود:
- **🔧 مهندس**: کمک فنی و برنامه‌نویسی
- **✈️ خلبان**: مدیریت پروژه و برنامه‌ریزی
- **👨‍✈️ مهماندار**: پشتیبانی و راهنمایی کاربران
- **📖 راهنما**: مشاوره استراتژیک
- **🏗️ معمار**: طراحی و ساخت عامل جدید (ویژگی جدید)

---

## 👥 برای کاربران عادی

### دسترسی از طریق ویجت چت

ویجت نجم‌هدا در **تمام صفحات سایت** (بعد از لاگین) نمایش داده می‌شود.

#### نحوه استفاده:
1. وارد هر صفحه از سایت شوید
2. در پایین سمت چپ صفحه یک دکمه دایره‌ای می‌بینید: 🌟
3. روی آن کلیک کنید
4. پنجره چت باز می‌شود
5. پیام خود را بنویسید و Enter بزنید

#### مثال‌های استفاده:
- "چطور میتونم یک مزایده جدید ایجاد کنم؟"
- "گروه من کجاست؟"
- "چطور تراکنش انجام بدم؟"

#### ویژگی‌های ویجت چت:
- انتخاب عامل دلخواه
- پیشنهادات سریع
- تاریخچه مکالمات
- سیستم بازخورد

#### نوع سوالاتی که می‌توانید بپرسید:
- **🔧 سوالات فنی**: خطاها، کد، بهترین روش‌ها
- **✈️ سوالات مدیریتی**: پروژه‌ها، برنامه کاری، کارها
- **👨‍✈️ سوالات پشتیبانی**: حساب، قسمت‌ها، راهنما
- **📖 سوالات استراتژیک**: استراتژی رشد، مشاوره

### دسترسی از طریق API (برای توسعه‌دهندگان)

#### Endpoint:
```
POST /api/najm-hoda/chat
```

#### Request:
```json
{
  "message": "سلام نجم‌هدا",
  "conversation_id": null,
  "agent": "steward"
}
```

#### Response:
```json
{
  "success": true,
  "message": "سلام! چطور میتونم کمکتون کنم؟",
  "agent": "steward",
  "conversation_id": "uuid-here",
  "suggestions": [
    "راهنمای استفاده",
    "سوالات متداول",
    "تماس با پشتیبانی"
  ]
}
```

---

## 👨‍💼 برای مدیران (ادمین)

### داشبورد مدیریت نجم‌هدا

#### دسترسی:
```
/admin/najm-hoda
```

#### امکانات:
- **📊 داشبورد اصلی**: آمار کلی، نمودارها، وضعیت عوامل
- **💬 مکالمات**: لیست مکالمات، جزئیات، فیلتر
- **📈 تحلیل‌ها**: نمودارها، آمار عوامل، هزینه‌ها، رضایت
- **⭐ بازخوردها**: نظرات، امتیازات، توزیع رضایت
- **⚙️ تنظیمات**: فعال/غیرفعال، مدل AI، API Key، محدودیت‌ها
- **💻 چت مستقیم**: چت با نجم‌هدا به عنوان ادمین
- **🏗️ ساخت عامل جدید**: طراحی با کمک معمار، تولید کد
- **📋 لاگ‌ها**: مشاهده لاگ‌ها، پاک کردن

### چت مستقیم با نجم‌هدا (ادمین)

#### دسترسی:
```
/admin/najm-hoda/chat
```

#### ویژگی‌های خاص ادمین:
- انتخاب دستی عامل
- دسترسی کامل به تمام قابلیت‌ها
- تست عوامل جدید
- شبیه‌سازی سناریوها

#### مثال‌های استفاده ادمین:
- "مهندس، کد Migration برای جدول products بساز"
- "خلبان، گزارش عملکرد پروژه این ماه رو بده"
- "معمار، عاملی برای مدیریت ایمیل‌ها بساز"

### ساخت عامل جدید

#### دسترسی:
```
/admin/najm-hoda/create-agent
```

#### مراحل:
1. توضیح نیاز بدهید
2. معمار نیاز را تحلیل می‌کند
3. طراحی عامل (نام، نقش، تخصص‌ها، متدها)
4. تولید کد خودکار
5. ذخیره فایل
6. راهنمای یکپارچه‌سازی

#### از طریق CLI:
```bash
php artisan najm-hoda:create-agent "توضیح نیاز خود"
```

#### مثال‌ها:
- عامل تولید محتوا
- عامل مدیریت پایگاه داده
- عامل تحلیل داده

### تنظیمات پیشرفته

#### دسترسی:
```
/admin/najm-hoda/settings
```

#### تنظیمات موجود:
```env
# فعالسازی نجم‌هدا
NAJM_HODA_ENABLED=true

# ارائه‌دهنده AI
AI_PROVIDER=openai
AI_MODEL=gpt-4-turbo-preview
AI_API_KEY=sk-your-key-here

# تنظیمات مدل
AI_MAX_TOKENS=2000
AI_TEMPERATURE=0.7

# محدودیت‌ها
NAJM_HODA_RATE_LIMIT=60
NAJM_HODA_MAX_CONVERSATION_LENGTH=50
NAJM_HODA_AUTO_ACTIONS=false
NAJM_HODA_REQUIRE_APPROVAL=true
```

### مانیتورینگ و لاگ‌ها

#### لاگ‌های سیستم:
```
/admin/najm-hoda/logs
```

#### اطلاعات ثبت شده:
- تعاملات با AI
- خطاها و هشدارها
- زمان پاسخ
- هزینه هر درخواست
- عامل استفاده شده

#### فایل لاگ:
```
storage/logs/najm-hoda.log
```

### تحلیل‌ها و آمار

#### دسترسی:
```
/admin/najm-hoda/analytics
```

#### گزارش‌های موجود:
- **📊 استفاده روزانه**: نمودار تعاملات، پیک استفاده
- **📈 آمار عوامل**: محبوب‌ترین عامل، هزینه هر عامل
- **💰 هزینه‌ها**: هزینه کل، روزانه/هفتگی/ماهانه
- **⭐ رضایت کاربران**: میانگین امتیاز، توزیع، نظرات

---

## 🏗️ معماری و ساختار فنی

### نمای کلی سیستم

```
┌─────────────────────────────────────────┐
│         کاربران (Users)                 │
│                                         │
│  🌐 ویجت چت در تمام صفحات              │
│  📱 /api/najm-hoda/chat                │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│      NajmHodaOrchestrator               │
│      (هماهنگ‌کننده مرکزی)                │
└────────────┬────────────────────────────┘
             │
    ┌────────┴────────┐
    │                 │
    ▼                 ▼
┌─────────┐      ┌─────────┐
│ Agents  │      │ Agents  │
│  🔧 👨‍✈️   │      │  📖 🏗️  │
│  ✈️ 📖   │      │         │
└─────────┘      └─────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│          ادمین (Admin)                  │
│                                         │
│  🖥️ داشبورد: /admin/najm-hoda          │
│  💬 چت مستقیم                           │
│  📊 تحلیل‌ها                             │
│  ⚙️ تنظیمات                              │
│  🏗️ ساخت عامل جدید                      │
└─────────────────────────────────────────┘
```

### ماژولاریتی

نجم‌هدا کاملاً ماژولار طراحی شده:

#### لایه‌ها:
1. **BaseAgent**: عملیات مشترک
2. **Specialized Agents**: عوامل تخصصی (Engineer, Pilot, Steward, Guide, Architect)
3. **Orchestrator**: مسیریابی هوشمند

#### مزایای معماری ماژولار:
- اضافه کردن عامل جدید بدون تغییر کدهای قبلی
- هر عامل مستقل کار می‌کند
- تست‌پذیری بالا
- نگهداری آسان
- توسعه موازی

### فایل‌های ایجاد شده

#### Backend:
- `app/Http/Controllers/Admin/NajmHodaController.php`
- `routes/web.php` (13 روت با prefix /admin/najm-hoda)

#### Frontend:
- `resources/views/admin/najm-hoda/index.blade.php`
- `resources/views/admin/najm-hoda/chat.blade.php`

#### تنظیمات:
- `config/najm-hoda.php`

#### ویجت:
- `resources/views/components/najm-hoda-widget.blade.php`

---

## 🚀 راه‌اندازی و نصب

### مرحله 1: اجرای Migration

```bash
php artisan migrate
```

این جداول را می‌سازد:
- conversations
- conversation_messages
- ai_interactions
- feedbacks
- roadmaps
- sprints
- sprint_tasks
- system_health_reports
- ai_code_generations

### مرحله 2: تنظیم API Key

فایل `.env`:
```env
NAJM_HODA_ENABLED=true
AI_PROVIDER=openai
AI_MODEL=gpt-4-turbo-preview
AI_API_KEY=sk-your-key-here
AI_MAX_TOKENS=2000
AI_TEMPERATURE=0.7
```

### مرحله 3: تست

#### برای کاربر:
1. لاگین کن
2. وارد هر صفحه شو
3. دکمه 🌟 در پایین چپ رو کلیک کن
4. پیام بفرست

#### برای ادمین:
1. برو به `/admin/najm-hoda`
2. روی "چت با نجم‌هدا" کلیک کن
3. یک سوال بپرس

#### از CLI:
```bash
php artisan najm-hoda:chat "سلام نجم‌هدا"
```

---

## 📡 مستندات API

### Endpoint اصلی چت:
```
POST /api/najm-hoda/chat
```

#### پارامترها:
- `message` (string, required): پیام کاربر
- `conversation_id` (string, optional): ID مکالمه موجود
- `agent` (string, optional): عامل مورد نظر (engineer, pilot, steward, guide, architect)

#### پاسخ موفق:
```json
{
  "success": true,
  "message": "پاسخ نجم‌هدا",
  "agent": "عامل استفاده شده",
  "conversation_id": "uuid مکالمه",
  "suggestions": ["پیشنهاد 1", "پیشنهاد 2"]
}
```

#### پاسخ خطا:
```json
{
  "success": false,
  "message": "پیام خطا",
  "error_code": "کد خطا"
}
```

### سایر Endpointها:
- `GET /api/najm-hoda/conversations` - لیست مکالمات
- `GET /api/najm-hoda/agents` - لیست عوامل
- `POST /api/najm-hoda/feedback` - ارسال بازخورد

---

## ❓ سوالات متداول (FAQ)

### آیا نجم‌هدا یک ماژول مستقل است؟
بله، نجم‌هدا نیمه‌مستقل طراحی شده. ساختار فایل‌ها، دیتابیس، تنظیمات و APIهای مجزا دارد اما از امکانات Laravel استفاده می‌کند.

### آیا نجم‌هدا ماژولار ساخته شده؟
بله، کاملاً ماژولار است. هر عامل مستقل کار می‌کند و اضافه کردن عامل جدید بدون تغییر کدهای قبلی امکان‌پذیر است.

### آیا نجم‌هدا می‌تواند عامل جدید بسازد؟
بله! با عامل Architect می‌تواند خودش را توسعه دهد. از طریق CLI یا چت می‌توانید عامل جدید بسازید.

#### مثال ساخت عامل:
```bash
php artisan najm-hoda:create-agent "عامل تولید محتوا و تبلیغات"
```

### چگونه از نجم‌هدا استفاده کنم؟
- **کاربران**: از ویجت چت در پایین صفحه استفاده کنید
- **ادمین**: از داشبورد `/admin/najm-hoda` استفاده کنید
- **توسعه‌دهندگان**: از API استفاده کنید

### هزینه‌های API چطور محاسبه می‌شود؟
هزینه‌ها بر اساس تعداد توکن‌های استفاده شده محاسبه می‌شود. می‌توانید در بخش تحلیل‌ها هزینه‌های روزانه/هفتگی/ماهانه را مشاهده کنید.

### آیا مکالمات ذخیره می‌شوند؟
بله، تمام مکالمات ذخیره می‌شوند و می‌توانید تاریخچه آن‌ها را مشاهده کنید.

---

## 🔧 بهبودها و به‌روزرسانی‌ها

### وضعیت فعلی صفحات
تمام صفحات ادمین نجم‌هدا (12 صفحه) ایجاد شده‌اند و از Tailwind CSS استفاده می‌کنند.

### مشکلات شناسایی شده و حل شده

#### Dark Mode:
- ✅ فایل CSS مشترک برای dark mode ایجاد شد
- ✅ اضافه کردن به layout ادمین
- ⚠️ نیاز به بهبود در برخی صفحات خاص

#### Responsive Design:
- ✅ استفاده از Tailwind CSS در تمام صفحات
- ⚠️ نیاز به بهبود جداول و چارت‌ها در موبایل

#### بهینه‌سازی:
- ✅ کاهش استایل‌های تکراری
- ⚠️ بهبود performance در موبایل

### فایل‌های بهبود یافته:
- `public/Css/najm-hoda-dark-mode.css` - استایل‌های dark mode و responsive
- اضافه شدن به `layouts/admin.blade.php`

---

## 🚀 برنامه‌های آینده

### نسخه 1.2.0 (نزدیک):
- یکپارچه‌سازی خودکار عامل جدید
- تست خودکار عوامل
- بررسی کد تولید شده

### نسخه 2.0.0 (دور):
- سیستم یادگیری از تعاملات
- همکاری چندعاملی
- بازار عوامل

---

## ⚠️ نکات مهم

### برای کاربران:
- پیام‌های شما محرمانه هستند
- تاریخچه مکالمات ذخیره می‌شود
- می‌توانید بازخورد بدهید
- نجم‌هدا 24/7 در دسترس است

### برای ادمین:
- همیشه API Key را محرمانه نگه دارید
- هزینه‌های API را مانیتور کنید
- بازخوردهای کاربران را بررسی کنید
- قبل از ساخت عامل جدید، تست کنید
- لاگ‌ها را به صورت دوره‌ای پاک کنید

---

## 📞 پشتیبانی و راهنمایی

### برای کاربران:
- از ویجت چت استفاده کنید
- سوال خود را از مهماندار بپرسید

### برای ادمین:
- داشبورد: `/admin/najm-hoda`
- مستندات: `/docs/najm-hoda`
- لاگ‌ها: `/admin/najm-hoda/logs`

---

## 📊 آمار فعلی

- 🤖 تعداد عوامل: 5 (Engineer, Pilot, Steward, Guide, Architect)
- 📁 فایل‌های کد: 20+
- 📊 جداول دیتابیس: 9
- 🔌 API Endpoints: 8
- 💬 سیستم چت: ✅ فعال
- 🏗️ قابلیت خودتوسعه: ✅ فعال

---

**نجم‌هدا - دستیار هوشمند همیشگی شما** 🌟

برای شروع:
1. Migration را اجرا کنید
2. API Key را تنظیم کنید
3. تست کنید!

اگر سوال یا مشکلی دارید، از طریق ویجت چت با ما تماس بگیرید.