# 📚 راهنمای کامل استفاده از نجم‌هدا

## برای کاربران عادی 👥

### 1️⃣ **دسترسی از طریق ویجت چت**

ویجت نجم‌هدا در **تمام صفحات سایت** (بعد از لاگین) نمایش داده می‌شود.

#### نحوه استفاده:

```
1. وارد هر صفحه از سایت شوید
2. در پایین سمت چپ صفحه یک دکمه دایره‌ای می‌بینید: 🌟
3. روی آن کلیک کنید
4. پنجره چت باز می‌شود
5. پیام خود را بنویسید و Enter بزنید
```

#### مثال‌های استفاده:

```
کاربر: "چطور میتونم یک مزایده جدید ایجاد کنم؟"
نجم‌هدا: "برای ایجاد مزایده جدید..."

کاربر: "گروه من کجاست؟"
نجم‌هدا: "گروه‌های شما در منوی بالا..."

کاربر: "چطور تراکنش انجام بدم؟"
نجم‌هدا: "برای انجام تراکنش..."
```

---

### 2️⃣ **ویژگی‌های ویجت چت**

#### ✅ انتخاب عامل:

```html
┌─────────────────────────────┐
│  نجم‌هدا 🌟                 │
├─────────────────────────────┤
│  🔧 مهندس                   │  ← کمک فنی
│  ✈️ خلبان                   │  ← راهنمای پروژه
│  👨‍✈️ مهماندار                │  ← پشتیبانی
│  📖 راهنما                   │  ← مشاوره
└─────────────────────────────┘
```

#### ✅ پیشنهادات سریع:

```
▪ چطور یک مزایده ایجاد کنم؟
▪ گروه من کجاست؟
▪ چطور موجودی شارژ کنم؟
```

#### ✅ تاریخچه مکالمات:

همه مکالمات شما ذخیره می‌شود و می‌تونید بعداً به آن‌ها برگردید.

---

### 3️⃣ **نوع سوالاتی که می‌تونید بپرسید:**

#### 🔧 سوالات فنی (عامل مهندس):
```
- "این خطا چیه؟"
- "کد این بخش رو بررسی کن"
- "بهترین روش برای... چیه؟"
```

#### ✈️ سوالات مدیریتی (عامل خلبان):
```
- "پروژه‌های من کجان؟"
- "برنامه کاری این هفته چیه؟"
- "چند تا کار عقب افتاده دارم؟"
```

#### 👨‍✈️ سوالات پشتیبانی (عامل مهماندار):
```
- "چطور میتونم یک حساب جدید بسازم؟"
- "این قسمت چطور کار میکنه؟"
- "راهنما بده برای..."
```

#### 📖 سوالات استراتژیک (عامل راهنما):
```
- "بهترین استراتژی برای رشد گروه من چیه؟"
- "چطور میتونم بیشتر فعال باشم؟"
- "پیشنهاد بده برای..."
```

---

### 4️⃣ **دسترسی از طریق API** (برای توسعه‌دهندگان)

#### Endpoint:
```
POST /api/najm-hoda/chat
```

#### Request:
```json
{
  "message": "سلام نجم‌هدا",
  "conversation_id": null,
  "agent": "steward"
}
```

#### Response:
```json
{
  "success": true,
  "message": "سلام! چطور میتونم کمکتون کنم؟",
  "agent": "steward",
  "conversation_id": "uuid-here",
  "suggestions": [
    "راهنمای استفاده",
    "سوالات متداول",
    "تماس با پشتیبانی"
  ]
}
```

---

## برای ادمین 👨‍💼

### 1️⃣ **داشبورد مدیریت نجم‌هدا**

#### دسترسی:
```
/admin/najm-hoda
```

#### امکانات:

```
📊 داشبورد اصلی:
   ├─ آمار کلی (مکالمات، تعاملات، هزینه‌ها)
   ├─ نمودار استفاده روزانه
   ├─ وضعیت عوامل
   └─ مکالمات اخیر

💬 مکالمات:
   ├─ لیست تمام مکالمات
   ├─ جزئیات هر مکالمه
   ├─ فیلتر بر اساس کاربر/عامل/تاریخ
   └─ جستجو در مکالمات

📈 تحلیل‌ها:
   ├─ نمودار استفاده روزانه/هفتگی/ماهانه
   ├─ آمار هر عامل
   ├─ هزینه‌های API
   ├─ میانگین زمان پاسخ
   └─ نرخ رضایت کاربران

⭐ بازخوردها:
   ├─ لیست تمام بازخوردها
   ├─ میانگین امتیاز
   ├─ توزیع امتیازات
   └─ نظرات کاربران

⚙️ تنظیمات:
   ├─ فعال/غیرفعال کردن نجم‌هدا
   ├─ انتخاب ارائه‌دهنده AI (OpenAI/Claude/Gemini)
   ├─ تنظیم مدل (GPT-4, GPT-3.5, ...)
   ├─ محدودیت نرخ درخواست
   ├─ فعالسازی اقدامات خودکار
   └─ مدیریت API Key

💻 چت مستقیم:
   ├─ چت با نجم‌هدا به عنوان ادمین
   ├─ انتخاب دستی عامل
   ├─ دسترسی به تمام قابلیت‌ها
   └─ تست عوامل

🏗️ ساخت عامل جدید:
   ├─ طراحی عامل با کمک Architect
   ├─ تولید کد خودکار
   ├─ ذخیره و یکپارچه‌سازی
   └─ تست عامل جدید

📋 لاگ‌ها:
   ├─ مشاهده لاگ‌های سیستم
   ├─ خطاها و هشدارها
   └─ پاک کردن لاگ‌ها
```

---

### 2️⃣ **چت مستقیم با نجم‌هدا (ادمین)**

#### دسترسی:
```
/admin/najm-hoda/chat
```

#### ویژگی‌های خاص ادمین:

```
✅ انتخاب دستی عامل:
   - می‌تونید عامل خاصی رو انتخاب کنید

✅ دسترسی کامل:
   - دسترسی به تمام قابلیت‌ها
   - مجوز انجام اقدامات حساس
   - دیدن اطلاعات پیشرفته

✅ تست و آزمایش:
   - تست عوامل جدید
   - بررسی عملکرد
   - شبیه‌سازی سناریوها
```

#### مثال‌های استفاده ادمین:

```
ادمین: "مهندس، کد Migration برای جدول products بساز"
مهندس: "Migration آماده شد..."

ادمین: "خلبان، گزارش عملکرد پروژه این ماه رو بده"
خلبان: "گزارش ماهانه..."

ادمین: "معمار، عاملی برای مدیریت ایمیل‌ها بساز"
معمار: "عامل EmailAgent در حال طراحی..."
```

---

### 3️⃣ **ساخت عامل جدید**

#### دسترسی:
```
/admin/najm-hoda/create-agent
```

#### مراحل:

```
1️⃣ توضیح نیاز:
   "نیاز به عاملی دارم که محتوا و تبلیغات تولید کنه"

2️⃣ تحلیل نیاز:
   معمار نجم‌هدا نیاز را تحلیل می‌کند
   └─ آیا نیاز به عامل جدید هست؟
   └─ کدوم عامل موجود می‌تونه این کار رو بکنه؟

3️⃣ طراحی عامل:
   ├─ نام: ContentAgent
   ├─ نقش: content_creator
   ├─ تخصص‌ها: [content_writing, advertising, seo]
   └─ متدها: [generateBlogPost, createAd, ...]

4️⃣ تولید کد:
   کد کامل PHP تولید می‌شود

5️⃣ ذخیره:
   فایل در app/Services/NajmHoda/Agents/ ذخیره می‌شود

6️⃣ راهنمای یکپارچه‌سازی:
   مراحل ثبت در Orchestrator و ServiceProvider
```

#### از طریق CLI:

```bash
php artisan najm-hoda:create-agent "توضیح نیاز خود"
```

---

### 4️⃣ **تنظیمات پیشرفته**

#### دسترسی:
```
/admin/najm-hoda/settings
```

#### تنظیمات موجود:

```env
# فایل .env

# فعالسازی نجم‌هدا
NAJM_HODA_ENABLED=true

# ارائه‌دهنده AI
AI_PROVIDER=openai          # openai, anthropic, google
AI_MODEL=gpt-4-turbo-preview
AI_API_KEY=sk-your-key-here

# تنظیمات مدل
AI_MAX_TOKENS=2000
AI_TEMPERATURE=0.7

# محدودیت‌ها
NAJM_HODA_RATE_LIMIT=60     # درخواست در دقیقه
NAJM_HODA_MAX_CONVERSATION_LENGTH=50

# قابلیت‌های پیشرفته
NAJM_HODA_AUTO_ACTIONS=false
NAJM_HODA_REQUIRE_APPROVAL=true
```

---

### 5️⃣ **مانیتورینگ و لاگ‌ها**

#### لاگ‌های سیستم:
```
/admin/najm-hoda/logs
```

#### اطلاعات ثبت شده:
```
- تمام تعاملات با AI
- خطاها و هشدارها
- زمان پاسخ
- هزینه هر درخواست
- عامل استفاده شده
```

#### فایل لاگ:
```
storage/logs/najm-hoda.log
```

---

### 6️⃣ **تحلیل‌ها و آمار**

#### دسترسی:
```
/admin/najm-hoda/analytics
```

#### گزارش‌های موجود:

```
📊 استفاده روزانه:
   - نمودار تعاملات 30 روز اخیر
   - پیک استفاده
   - ساعات شلوغی

📈 آمار عوامل:
   - محبوب‌ترین عامل
   - میانگین توکن هر عامل
   - هزینه هر عامل

💰 هزینه‌ها:
   - هزینه کل API
   - هزینه روزانه/هفتگی/ماهانه
   - پیش‌بینی هزینه

⭐ رضایت کاربران:
   - میانگین امتیاز
   - توزیع امتیازات
   - نظرات برگزیده
```

---

## روش‌های ارتباط با نجم‌هدا

### 1️⃣ کاربران:
```
✅ ویجت چت (در تمام صفحات)
✅ API (برای توسعه‌دهندگان)
```

### 2️⃣ ادمین:
```
✅ ویجت چت (با قابلیت‌های بیشتر)
✅ داشبورد ادمین (/admin/najm-hoda)
✅ چت مستقیم (/admin/najm-hoda/chat)
✅ CLI (php artisan najm-hoda:chat)
✅ API (با دسترسی کامل)
```

---

## نکات مهم ⚠️

### برای کاربران:
```
1. پیام‌های شما محرمانه هستند
2. تاریخچه مکالمات ذخیره می‌شود
3. می‌تونید بازخورد بدید (امتیاز + نظر)
4. نجم‌هدا 24/7 در دسترس است
```

### برای ادمین:
```
1. همیشه API Key رو محرمانه نگه دارید
2. هزینه‌های API رو مانیتور کنید
3. بازخوردهای کاربران رو بررسی کنید
4. قبل از ساخت عامل جدید، تست کنید
5. لاگ‌ها رو به صورت دوره‌ای پاک کنید
```

---

## مثال‌های کاربردی

### سناریو 1: کاربر مبتدی
```
کاربر: "سلام، تازه عضو شدم. چکار کنم؟"
مهماندار: "سلام! خوش اومدید. بذارید راهنماتون کنم..."
          [آموزش گام به گام]
```

### سناریو 2: ادمین می‌خواد کد بنویسه
```
ادمین: "مهندس، یک Controller برای مدیریت محصولات بساز"
مهندس: "Controller ProductController آماده شد:
        [کد کامل]"
```

### سناریو 3: ادمین می‌خواد عامل جدید بسازه
```
ادمین: "معمار، عاملی برای ارسال ایمیل و SMS بساز"
معمار: "عامل NotificationAgent در حال طراحی...
        [طراحی کامل + کد]"
```

### سناریو 4: کاربر نیاز به مشاوره داره
```
کاربر: "چطور میتونم گروهم رو بزرگ کنم؟"
راهنما: "برای رشد گروه این استراتژی‌ها رو پیشنهاد میدم:
        1. محتوای منظم تولید کنید
        2. با اعضا تعامل داشته باشید
        3. ..."
```

---

## پشتیبانی و راهنمایی

### برای کاربران:
```
- از ویجت چت استفاده کنید
- سوال خود را از مهماندار بپرسید
```

### برای ادمین:
```
- داشبورد: /admin/najm-hoda
- مستندات: /docs/najm-hoda
- لاگ‌ها: /admin/najm-hoda/logs
```

---

**نجم‌هدا - دستیار هوشمند همیشگی شما** 🌟
