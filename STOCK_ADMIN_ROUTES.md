# 📍 راهنمای آدرس‌های مدیریت سهام و حراج

## 🏠 صفحه اصلی مدیریت سهام
**آدرس:** `/admin/stock`  
**توضیح:** نمایش اطلاعات پایه سهام، آمار و نمودارها، هشدارها

## 🎯 مدیریت حراج‌ها

### لیست حراج‌ها
**آدرس:** `/admin/auctions`  
**توضیح:** لیست کامل حراج‌ها با فیلترهای پیشرفته و عملیات دسته‌ای

### ایجاد حراج جدید
**آدرس:** `/admin/auctions/create`  
**توضیح:** فرم ایجاد حراج جدید

### ویرایش حراج
**آدرس:** `/admin/auctions/{id}/edit`  
**توضیح:** ویرایش اطلاعات حراج

### نمایش جزئیات حراج
**آدرس:** `/admin/auctions/{id}`  
**توضیح:** نمایش کامل اطلاعات حراج و صف خرید

### شروع حراج
**آدرس:** `POST /admin/auctions/{id}/start`  
**توضیح:** شروع یک حراج برنامه‌ریزی شده

### بستن حراج
**آدرس:** `POST /admin/auctions/{id}/close`  
**توضیح:** بستن و تسویه یک حراج فعال

### حذف حراج
**آدرس:** `DELETE /admin/auctions/{id}`  
**توضیح:** حذف یک حراج

### Export CSV حراج
**آدرس:** `/admin/auctions/{id}/export`  
**توضیح:** دانلود اطلاعات حراج به صورت CSV

### عملیات دسته‌ای حراج‌ها
**آدرس:** `POST /admin/auctions/bulk-action`  
**پارامترها:**
- `action`: `start`, `close`, `delete`
- `auction_ids[]`: آرایه‌ای از شناسه‌های حراج

## 📊 گزارش‌گیری

### گزارش عملکرد حراج‌ها
**آدرس:** `/admin/stock-reports/auction-performance`  
**پارامترهای اختیاری:**
- `date_from`: تاریخ شروع (مثلاً `1404/01/01`)
- `date_to`: تاریخ پایان (مثلاً `1404/12/29`)

**Export CSV:**
**آدرس:** `/admin/stock-reports/auction-performance/export`

### گزارش سرمایه‌گذاران
**آدرس:** `/admin/stock-reports/investors`  
**توضیح:** لیست کامل سرمایه‌گذاران و میزان سرمایه‌گذاری

**Export CSV:**
**آدرس:** `/admin/stock-reports/investors/export`

### گزارش مالی
**آدرس:** `/admin/stock-reports/financial`  
**پارامترهای اختیاری:**
- `date_from`: تاریخ شروع
- `date_to`: تاریخ پایان

**Export CSV:**
**آدرس:** `/admin/stock-reports/financial/export`

## 🔧 تنظیمات

### ویرایش اطلاعات پایه سهام
**آدرس:** `/admin/stock/create`  
**توضیح:** فرم ویرایش/ایجاد اطلاعات پایه سهام

## ⚠️ مشکلات شناخته شده

1. **عملیات دسته‌ای کار نمی‌کند:**
   - بررسی کنید که route در `routes/web.php` درست تعریف شده باشد
   - بررسی کنید که permission middleware درست اعمال شده باشد
   - در console مرورگر بررسی کنید که آیا خطای JavaScript وجود دارد

2. **آمار و نمودارها نمایش داده نمی‌شوند:**
   - بررسی کنید که Chart.js در صفحه لود شده باشد
   - بررسی کنید که متغیرهای `$stats` و `$chart_data` از controller پاس داده شده باشند

