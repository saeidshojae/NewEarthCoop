# 📘 راهنمای جامع Mock Mode

## Mock Mode چیست؟

Mock Mode یک حالت شبیه‌سازی است که به شما اجازه می‌دهد سیستم نجم‌هدا را **بدون نیاز به API واقعی** تست کنید.

---

## ✅ مزایای Mock Mode

### 1. **صرفه‌جویی در هزینه**
- هیچ هزینه‌ای برای API پرداخت نمی‌کنید
- مناسب برای Development و Testing

### 2. **سرعت بالا**
- پاسخ‌ها فوری هستند
- نیازی به انتظار برای پردازش AI نیست

### 3. **بدون نیاز به اینترنت**
- کار کردن Offline امکان‌پذیر است
- مناسب برای شرایط فیلترینگ

### 4. **تست کامل UI/UX**
- تمام بخش‌های رابط کاربری قابل تست است
- مشاهده نحوه نمایش پاسخ‌ها

---

## ⚙️ فعال‌سازی Mock Mode

### روش 1: از طریق فایل `.env`

```env
NAJM_HODA_MOCK_MODE=true
```

### روش 2: از طریق پنل ادمین

1. به `/admin/najm-hoda/settings` بروید
2. گزینه "Mock Mode" را فعال کنید
3. تنظیمات را ذخیره کنید

### روش 3: از طریق کد

```php
config(['najm-hoda.mock_mode' => true]);
```

---

## 🎭 انواع پاسخ‌های Mock

### 1️⃣ مهندس (Engineer)

#### Code Review
**ورودی:** بررسی این کد
```php
public function getUsers() {
    return User::all();
}
```

**پاسخ Mock:**
```
✅ **کد قابل قبول است**

### نکات مثبت:
- ساختار کلاس منظم و خوانا
- استفاده صحیح از Type Hinting

### پیشنهادات بهبود:
1. افزودن Unit Test
2. استفاده از Dependency Injection
3. Exception Handling

**امتیاز کلی:** 8/10
```

#### Bug Analysis
**ورودی:** این باگ رو چک کن
**پاسخ Mock:** تحلیل علت و راه‌حل

#### Refactoring
**ورودی:** این کد رو بهینه کن
**پاسخ Mock:** کد بازنویسی شده

---

### 2️⃣ خلبان (Pilot)

#### Implement Feature
**ورودی:** سیستم نظرات رو پیاده کن
**پاسخ Mock:**
```
📋 **طرح پیاده‌سازی**

### مرحله 1: آماده‌سازی (2 ساعت)
- بررسی نیازمندی‌ها
- طراحی Database
- ایجاد Migration

### مرحله 2: Backend (4 ساعت)
- Model و Controller
- Business Logic
- API Endpoints

...
```

#### Task Breakdown
**ورودی:** این تسک رو تقسیم کن
**پاسخ Mock:** لیست Subtask‌ها با زمان‌بندی

---

### 3️⃣ مهماندار (Steward)

#### General Help
**ورودی:** سلام
**پاسخ Mock:**
```
سلام! 👋
خوشحالم که می‌تونم کمکتون کنم.

**من می‌تونم در این موارد کمک کنم:**
- پاسخ به سوالات عمومی
- راهنمایی سیستم
...
```

#### User Support
**ورودی:** مشکل دارم
**پاسخ Mock:** راهنمایی گام به گام حل مشکل

---

### 4️⃣ راهنما (Guide)

#### Documentation
**ورودی:** راهنمای نصب
**پاسخ Mock:** مستندات کامل با کد

#### Tutorial
**ورودی:** آموزش CRUD
**پاسخ Mock:** آموزش گام به گام

---

### 5️⃣ معمار (Architect)

#### Need Analysis
**ورودی:** نیاز به سیستم مدیریت محتوا
**پاسخ Mock:**
```
🔍 **تحلیل نیاز**

### بررسی عوامل فعلی:
❌ هیچ عاملی این نیاز را پوشش نمی‌دهد

### نتیجه:
✅ نیاز به عامل جدید: ContentManager
```

#### Agent Design
**ورودی:** طراحی عامل جدید
**پاسخ Mock:** کد کامل کلاس عامل

---

## 🔍 تشخیص خودکار نوع درخواست

MockModeService به صورت هوشمند نوع درخواست را تشخیص می‌دهد:

```php
// کلمات کلیدی
'review', 'بررسی', 'کد' → Code Review
'bug', 'باگ', 'خطا' → Bug Analysis
'refactor', 'بهینه' → Refactoring
'implement', 'پیاده' → Implementation
...
```

---

## 📊 مثال‌های استفاده

### مثال 1: تست چت

```bash
# ارسال پیام به مهندس
POST /api/najm-hoda/chat
{
    "message": "این کد رو بررسی کن: ...",
    "agent": "engineer"
}

# پاسخ
{
    "success": true,
    "message": "⚙️ **[Mock Mode - تست بدون API]**\n\n✅ کد قابل قبول...",
    "agent": "engineer",
    "mock_mode": true
}
```

### مثال 2: تست از Panel

1. برو `/admin/najm-hoda/chat`
2. یک عامل انتخاب کن
3. پیام بفرست
4. پاسخ Mock دریافت کن

### مثال 3: تست Widget کاربر

1. به صفحه اصلی برو
2. دکمه چت شناور پایین صفحه
3. با عوامل مختلف چت کن

---

## ⚠️ محدودیت‌های Mock Mode

### ❌ چیزهایی که کار **نمی‌کنند**:

1. **یادگیری واقعی**
   - پاسخ‌ها از پیش تعریف شده
   - سفارشی‌سازی بر اساس Context ندارد

2. **تحلیل واقعی کد**
   - فقط پاسخ‌های نمونه
   - بررسی عمیق انجام نمی‌شود

3. **تولید کد جدید**
   - کدها از قبل نوشته شده
   - خلاقیت AI واقعی ندارد

### ✅ چیزهایی که کار **می‌کنند**:

1. **تست UI/UX کامل**
2. **فلوی کامل مکالمه**
3. **ذخیره Conversations**
4. **تمام بخش‌های Admin Panel**
5. **Widget کاربران**

---

## 🔄 تبدیل به Production Mode

وقتی آماده استفاده واقعی شدید:

### گام 1: API Key واقعی

```env
AI_API_KEY=sk-your-real-openai-key
```

### گام 2: غیرفعال کردن Mock

```env
NAJM_HODA_MOCK_MODE=false
```

### گام 3: تست اتصال

```bash
php artisan tinker
```

```php
$orchestrator = app(\App\Services\NajmHoda\NajmHodaOrchestrator::class);
$response = $orchestrator->route('سلام نجم‌هدا');
dd($response);
```

---

## 💡 نکات مهم

### 1. **همیشه بررسی کنید**
```php
if (config('najm-hoda.mock_mode')) {
    // Mock Mode فعال است
}
```

### 2. **پیام‌ها شامل Badge هستند**
```
⚙️ **[Mock Mode - تست بدون API]**
```

### 3. **در Production غیرفعال کنید**
- Mock Mode فقط برای Development
- هرگز در Production فعال نگذارید

### 4. **Cache را پاک کنید**
```bash
php artisan config:clear
php artisan cache:clear
```

---

## 📈 گزارش عملکرد Mock

```php
$mockService = app(\App\Services\NajmHoda\MockModeService::class);
$report = $mockService->generatePerformanceReport();

// نتیجه
[
    'total_requests' => 250,
    'success_rate' => '95%',
    'avg_response_time' => '450ms',
    'mock_mode' => true,
    'agents_activity' => [
        'engineer' => 80,
        'pilot' => 60,
        'steward' => 70,
        'guide' => 30,
        'architect' => 10
    ]
]
```

---

## 🎯 موارد استفاده

### ✅ مناسب برای:
- Development محلی
- Testing UI/UX
- Demo برای کلاینت
- آموزش تیم
- بدون VPN در ایران

### ❌ نامناسب برای:
- Production
- تحلیل واقعی کد
- تولید محتوای واقعی
- یادگیری ماشین

---

## 🆘 رفع مشکلات

### مشکل 1: پاسخ Mock نمی‌آید
**راه‌حل:**
```bash
php artisan config:clear
php artisan cache:clear
```

### مشکل 2: Mock Mode فعال نمی‌شود
**بررسی:**
```php
// در .env
NAJM_HODA_MOCK_MODE=true

// تست
dd(config('najm-hoda.mock_mode')); // باید true باشد
```

### مشکل 3: پاسخ‌ها نامناسب
**راه‌حل:** سفارشی‌سازی در `MockModeService.php`

---

## 📚 منابع بیشتر

- `app/Services/NajmHoda/MockModeService.php` - کد اصلی
- `config/najm-hoda.php` - تنظیمات
- `/admin/najm-hoda/settings` - پنل تنظیمات

---

**نکته پایانی:** Mock Mode یک ابزار قدرتمند برای توسعه و تست است، اما **هرگز** جایگزین AI واقعی نیست! 🚀
