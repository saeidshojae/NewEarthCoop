# ماژول وبلاگ - NewEarthCoop

## 📖 معرفی

این ماژول یک سیستم مدیریت وبلاگ کامل و حرفه‌ای برای پروژه NewEarthCoop است که با معماری ماژولار Laravel ساخته شده است.

## ✨ ویژگی‌ها

### 🎯 مدیریت مقالات
- ✅ ایجاد، ویرایش، حذف و نمایش مقالات
- ✅ وضعیت‌های مختلف: پیش‌نویس، منتشر شده، بایگانی
- ✅ تصویر شاخص برای هر مقاله
- ✅ مقالات ویژه (Featured)
- ✅ محاسبه خودکار زمان مطالعه
- ✅ شمارنده بازدید
- ✅ نرم‌افزار حذف (Soft Delete)

### 📁 دسته‌بندی و برچسب‌ها
- ✅ مدیریت کامل دسته‌بندی‌ها
- ✅ مدیریت برچسب‌ها (Tags)
- ✅ رابطه Many-to-Many بین مقالات و برچسب‌ها
- ✅ تصویر و توضیحات برای دسته‌بندی‌ها

### 💬 سیستم نظرات
- ✅ نظرات با سیستم تایید (Approval)
- ✅ پاسخ به نظرات (Nested Comments)
- ✅ مدیریت نظرات در پنل ادمین
- ✅ تایید، رد و حذف نظرات

### 🔍 جستجو و فیلتر
- ✅ جستجوی پیشرفته در عنوان و محتوا
- ✅ فیلتر بر اساس دسته‌بندی
- ✅ فیلتر بر اساس برچسب
- ✅ مرتب‌سازی بر اساس تاریخ و محبوبیت

### 📊 پنل مدیریت
- ✅ داشبورد با آمار کامل
- ✅ مدیریت آسان مقالات
- ✅ مدیریت دسته‌بندی‌ها و برچسب‌ها
- ✅ مدیریت نظرات
- ✅ نمایش فعالیت‌های اخیر

### 🎨 رابط کاربری
- ✅ طراحی ریسپانسیو با Bootstrap
- ✅ نمایش زیبا مقالات
- ✅ صفحه تک مقاله با مقالات مرتبط
- ✅ سایدبار با دسته‌بندی‌ها و محبوب‌ترین‌ها
- ✅ Cloud برچسب‌ها

### 🔒 امنیت
- ✅ احراز هویت برای ثبت نظر
- ✅ Authorization برای پنل ادمین
- ✅ Validation کامل برای تمام فرم‌ها
- ✅ CSRF Protection
- ✅ XSS Protection

### 📱 SEO
- ✅ فیلدهای Meta Title, Description, Keywords
- ✅ URL های SEO Friendly (Slug)
- ✅ Sitemap Ready
- ✅ Structured Data Ready

## 📂 ساختار فایل‌ها

```
app/Modules/Blog/
├── Controllers/
│   ├── BlogController.php          # کنترلر فرانت‌اند
│   └── AdminBlogController.php     # کنترلر پنل ادمین
├── Models/
│   ├── Post.php                    # مدل مقالات
│   ├── BlogCategory.php            # مدل دسته‌بندی
│   ├── BlogTag.php                 # مدل برچسب
│   └── BlogComment.php             # مدل نظرات
├── Migrations/
│   ├── 2024_01_01_000001_create_blog_categories_table.php
│   ├── 2024_01_01_000002_create_blog_tags_table.php
│   ├── 2024_01_01_000003_create_blog_posts_table.php
│   ├── 2024_01_01_000004_create_blog_post_tag_table.php
│   └── 2024_01_01_000005_create_blog_comments_table.php
├── Requests/
│   ├── PostRequest.php             # ولیدیشن مقالات
│   ├── CategoryRequest.php         # ولیدیشن دسته‌بندی
│   └── CommentRequest.php          # ولیدیشن نظرات
├── Views/
│   ├── admin/                      # ویوهای پنل ادمین
│   │   ├── dashboard.blade.php
│   │   └── posts/
│   │       └── index.blade.php
│   └── frontend/                   # ویوهای فرانت‌اند
│       ├── index.blade.php         # لیست مقالات
│       └── show.blade.php          # صفحه تک مقاله
└── Services/                       # سرویس‌ها (در صورت نیاز)
```

## 🚀 نصب و راه‌اندازی

### 1. کپی کردن Migrations

```bash
# کپی migrations به پوشه اصلی database
cp app/Modules/Blog/Migrations/*.php database/migrations/
```

### 2. اجرای Migrations

```powershell
php artisan migrate
```

### 3. ایجاد دیتای نمونه (اختیاری)

در فایل `database/seeders/DatabaseSeeder.php` می‌توانید Seeder برای دیتای نمونه اضافه کنید:

```php
// دسته‌بندی نمونه
BlogCategory::create([
    'name' => 'تکنولوژی',
    'slug' => 'technology',
    'description' => 'مقالات مرتبط با تکنولوژی',
    'is_active' => true,
    'order' => 1
]);

// برچسب نمونه
BlogTag::create(['name' => 'Laravel', 'slug' => 'laravel']);

// مقاله نمونه
Post::create([
    'title' => 'اولین مقاله وبلاگ',
    'slug' => 'first-blog-post',
    'content' => 'محتوای مقاله...',
    'category_id' => 1,
    'user_id' => 1,
    'status' => 'published',
    'published_at' => now()
]);
```

### 4. دسترسی‌ها

مطمئن شوید پوشه‌های زیر قابل نوشتن هستند:
```
public/images/blog/posts/
public/images/blog/categories/
```

## 🌐 مسیرها (Routes)

### مسیرهای عمومی

- **صفحه اصلی وبلاگ**: `/blog`
- **نمایش مقاله**: `/blog/{slug}`
- **دسته‌بندی**: `/blog/category/{slug}`
- **برچسب**: `/blog/tag/{slug}`
- **جستجو**: `/blog/search?q=...`

### مسیرهای پنل ادمین

- **داشبورد**: `/admin/blog/dashboard`
- **مقالات**: `/admin/blog/posts`
- **دسته‌بندی‌ها**: `/admin/blog/categories`
- **برچسب‌ها**: `/admin/blog/tags`
- **نظرات**: `/admin/blog/comments`

## 🔧 تنظیمات

### Middleware ها

پنل ادمین از Middleware های زیر استفاده می‌کند:
- `auth`: احراز هویت کاربر
- `AdminMiddleware`: بررسی دسترسی ادمین

### Namespace های View

در `AppServiceProvider.php` ثبت شده:
```php
View::addNamespace('Blog', base_path('app/Modules/Blog/Views'));
```

## 📝 نحوه استفاده

### ایجاد مقاله جدید

1. وارد پنل ادمین شوید
2. به `/admin/blog/posts` بروید
3. روی "مقاله جدید" کلیک کنید
4. فرم را پر کرده و ذخیره کنید

### مدیریت نظرات

1. به `/admin/blog/comments` بروید
2. نظرات در انتظار تایید را مشاهده کنید
3. نظر را تایید، رد یا حذف کنید

### افزودن لینک به منوی سایت

در فایل لیاوت اصلی:
```blade
<a href="{{ route('blog.index') }}" class="nav-link">وبلاگ</a>
```

## 🎨 سفارشی‌سازی

### تغییر تعداد مقالات در صفحه

در `BlogController.php`:
```php
$posts = $query->paginate(12); // تغییر عدد 12
```

### تغییر استایل

فایل‌های CSS سفارشی را در:
```
resources/css/blog.css
```

### افزودن فیلدهای جدید

1. Migration جدید ایجاد کنید
2. فیلد را به `$fillable` مدل اضافه کنید
3. فرم‌ها و ویوها را به‌روزرسانی کنید

## 🐛 عیب‌یابی

### خطای 404 در روت‌ها
```bash
php artisan route:clear
php artisan config:clear
```

### خطای View not found
مطمئن شوید namespace در `AppServiceProvider` ثبت شده است.

### خطای دسترسی به تصاویر
```bash
# دسترسی write به پوشه images
chmod -R 775 public/images/blog/
```

## 📊 بهینه‌سازی

### Cache کردن مقالات پرمخاطب
```php
Cache::remember('popular_posts', 3600, function() {
    return Post::published()->popular(5)->get();
});
```

### Eager Loading
برای بهبود performance از eager loading استفاده شده:
```php
Post::with(['category', 'author', 'tags'])->get();
```

## 🔄 به‌روزرسانی‌های آینده

- [ ] ویرایشگر WYSIWYG (TinyMCE یا CKEditor)
- [ ] آپلود چند تصویر در مقاله
- [ ] سیستم لایک و دیسلایک
- [ ] اشتراک‌گذاری در شبکه‌های اجتماعی
- [ ] RSS Feed
- [ ] Sitemap XML
- [ ] نسخه API
- [ ] Newsletter
- [ ] Related Posts بر اساس AI

## 📞 پشتیبانی

برای سوالات و مشکلات:
- Issues در GitHub
- ایمیل به تیم توسعه

## 📄 لایسنس

این ماژول تحت لایسنس MIT منتشر شده است.

---

**توسعه داده شده برای پروژه NewEarthCoop** 🚀
