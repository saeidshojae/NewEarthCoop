# 🌍 سیستم چند زبانه (Multi-Language System)

## ✅ تغییرات انجام شده

### 1. **Middleware و Controller**
- ✅ `SetLocale` middleware ایجاد شد
- ✅ `LocaleController` برای تغییر زبان
- ✅ Middleware به `Kernel.php` اضافه شد

### 2. **Routes**
- ✅ `/lang/{locale}` - تغییر زبان
- ✅ `/lang/current` - دریافت زبان فعلی (API)

### 3. **فایل‌های زبان**
```
lang/
├── fa/          (فارسی - کامل)
│   ├── langFront.php
│   ├── langPanel.php
│   └── auth.php
├── en/          (انگلیسی - پایه)
│   ├── langFront.php
│   ├── langPanel.php
│   └── auth.php
└── ar/          (عربی - کپی از فارسی)
    ├── langFront.php
    ├── langPanel.php
    └── auth.php
```

### 4. **RTL/LTR Support**
- ✅ تگ `<html>` با `dir` attribute داینامیک
- ✅ CSS برای RTL/LTR در `app.blade.php`
- ✅ فایل CSS جداگانه: `public/Css/lang-direction.css`

### 5. **Language Switcher**
- ✅ دکمه انتخاب زبان در navbar
- ✅ نمایش پرچم و نام زبان فعلی
- ✅ Dropdown با 3 زبان: فارسی، انگلیسی، عربی

### 6. **Helper Functions**
فایل: `app/Helpers/LanguageHelper.php`

```php
get_direction()      // 'rtl' یا 'ltr'
is_rtl()            // true/false
is_ltr()            // true/false
get_locale_name()   // 'فارسی', 'English', 'العربية'
get_locale_flag()   // '🇮🇷', '🇬🇧', '🇸🇦'
```

---

## 📖 نحوه استفاده

### تغییر زبان توسط کاربر
کاربر می‌تواند از navbar روی دکمه زبان کلیک کند و زبان مورد نظر را انتخاب کند.

### استفاده از ترجمه‌ها در Blade
```php
<!-- روش 1: استفاده مستقیم -->
{{ __('langFront.home') }}
{{ __('langPanel.dashboard') }}

<!-- روش 2: با متغیر -->
@lang('langFront.welcome')

<!-- روش 3: با پارامتر -->
{{ __('langFront.hello_name', ['name' => $user->name]) }}
```

### چک کردن جهت زبان در Blade
```php
@if(is_rtl())
    <div class="text-right">محتوای راست‌چین</div>
@else
    <div class="text-left">Left-aligned content</div>
@endif
```

### استفاده از Helper Functions
```php
<!-- نمایش نام زبان فعلی -->
<span>{{ get_locale_name() }}</span>

<!-- نمایش پرچم -->
<span>{{ get_locale_flag() }}</span>

<!-- جهت متن -->
<div dir="{{ get_direction() }}">
    محتوا
</div>
```

---

## 🎨 سفارشی‌سازی CSS برای RTL/LTR

### در فایل Blade
```css
[dir="rtl"] .my-element {
    margin-right: 20px;
}

[dir="ltr"] .my-element {
    margin-left: 20px;
}
```

### استفاده از متغیر $direction
```php
<style>
    .my-class {
        text-align: {{ $direction == 'rtl' ? 'right' : 'left' }};
    }
</style>
```

---

## 🔧 افزودن ترجمه جدید

### 1. اضافه کردن به فایل فارسی
`lang/fa/langFront.php`:
```php
'my_new_key' => 'متن فارسی',
```

### 2. اضافه کردن به فایل انگلیسی
`lang/en/langFront.php`:
```php
'my_new_key' => 'English Text',
```

### 3. اضافه کردن به فایل عربی
`lang/ar/langFront.php`:
```php
'my_new_key' => 'النص العربي',
```

### 4. استفاده در Blade
```php
{{ __('langFront.my_new_key') }}
```

---

## 🌐 افزودن زبان جدید

### مراحل اضافه کردن زبان جدید (مثال: اسپانیایی)

1. **ایجاد پوشه زبان:**
```bash
mkdir lang/es
```

2. **کپی فایل‌های ترجمه:**
```bash
Copy-Item lang/en/* lang/es/
```

3. **ترجمه محتوا** در فایل‌های `lang/es/*.php`

4. **اضافه کردن به Middleware:**
`app/Http/Middleware/SetLocale.php`:
```php
$availableLocales = ['fa', 'en', 'ar', 'es'];
```

5. **اضافه کردن به Controller:**
`app/Http/Controllers/LocaleController.php`:
```php
$availableLocales = ['fa', 'en', 'ar', 'es'];
```

6. **اضافه کردن به Language Switcher:**
`resources/views/layouts/app.blade.php`:
```php
<li>
    <a class="dropdown-item" href="{{ route('locale.change', 'es') }}">
        🇪🇸 Español
    </a>
</li>
```

7. **اضافه کردن به Helper:**
`app/Helpers/LanguageHelper.php`:
```php
$names = [
    'fa' => 'فارسی',
    'en' => 'English',
    'ar' => 'العربية',
    'es' => 'Español'
];
```

---

## 🧪 تست سیستم

### 1. تست تغییر زبان
- بروید به صفحه اصلی
- روی دکمه زبان در navbar کلیک کنید
- زبان انگلیسی را انتخاب کنید
- صفحه باید به سمت چپ تراز شود

### 2. تست RTL/LTR
- در زبان فارسی: همه چیز راست‌چین
- در زبان انگلیسی: همه چیز چپ‌چین
- در زبان عربی: همه چیز راست‌چین

### 3. تست Session
- زبان را تغییر دهید
- صفحه را refresh کنید
- زبان باید همان زبان انتخابی باقی بماند

---

## ⚠️ نکات مهم

### 1. فایل‌های عربی
فایل‌های عربی فعلاً کپی از فارسی هستند. برای استفاده واقعی باید ترجمه شوند.

### 2. Bootstrap RTL
اگر نیاز به پشتیبانی کامل‌تر Bootstrap RTL دارید:
```html
@if(is_rtl())
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css">
@else
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
@endif
```

### 3. تمیز کردن Cache
بعد از هر تغییر در فایل‌های ترجمه:
```bash
php artisan cache:clear
php artisan config:clear
php artisan view:clear
```

### 4. متن‌های هاردکد
متن‌های هاردکد شده در فایل‌های Blade را پیدا کنید و با `__()` جایگزین کنید:
```php
<!-- قبل -->
<button>ورود</button>

<!-- بعد -->
<button>{{ __('langFront.login') }}</button>
```

---

## 🚀 مراحل بعدی (پیشنهادی)

1. ✅ **تکمیل ترجمه‌های انگلیسی** - همه کلیدها را ترجمه کنید
2. ✅ **ترجمه واقعی عربی** - متن‌های عربی درست
3. 🔲 **جایگزینی متن‌های هاردکد** - در تمام view ها
4. 🔲 **تست کامل** - تمام صفحات با هر 3 زبان
5. 🔲 **بهینه‌سازی CSS** - استایل‌های بهتر برای LTR
6. 🔲 **مستندسازی کد** - کامنت‌گذاری

---

## 📞 پشتیبانی

در صورت بروز مشکل:
1. Cache را پاک کنید
2. Middleware در `Kernel.php` ثبت شده باشد
3. Helper در `composer.json` ثبت شده باشد
4. `composer dump-autoload` را اجرا کنید

---

**✨ سیستم چند زبانه با موفقیت پیاده‌سازی شد!**
